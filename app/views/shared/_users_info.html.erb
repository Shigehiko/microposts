<div class="panel panel-default">
<div class="panel-body">
        <% @users.each do |user| %>
     <h3><%= link_to gravatar_for(user, size: 60), user %><%= user.name %></h3>
    <p><%= pluralize(user.microposts.count, "micropost") %></p>
  <% end %>
</div>
</div>

